# Build-time (Vite) variables for `bun run build --mode staging`.
# Non-secret. Secrets belong in .dev.vars / wrangler secrets.
#
# Staging reads images from the production bucket until a staging bucket
# exists; switch this to the staging bucket URL once it does.
VITE_PHOTO_BASE_URL=https://s3.amazonaws.com/robin-photos/
